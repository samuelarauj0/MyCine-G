
{
  "name": "mycine-g",
  "version": "1.0.0",
  "description": "MyCine G - Gamified movie and series rating platform",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "npm run docker:up && npm run backend:migrate && npm run backend:seed && concurrently \"npm run backend:dev\" \"npm run frontend:dev\"",
    "docker:up": "docker-compose up -d",
    "docker:down": "docker-compose down",
    "backend:dev": "cd backend && npm run start:dev",
    "frontend:dev": "cd frontend && npm run dev",
    "backend:migrate": "cd backend && npx prisma migrate dev",
    "backend:seed": "cd backend && npx prisma db seed",
    "backend:generate": "cd backend && npx prisma generate",
    "test": "npm run test --workspaces",
    "lint": "npm run lint --workspaces",
    "format": "npm run format --workspaces"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
